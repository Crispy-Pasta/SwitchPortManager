# =================================================================
# Dell Switch Port Tracer v2.1.8 - Environment Configuration
# =================================================================
# Copy this file to .env and configure for your environment
# SECURITY WARNING: Never commit real credentials to version control!

# =================================================================
# DATABASE CONFIGURATION (Required)
# =================================================================
POSTGRES_DB=port_tracer_db
POSTGRES_USER=dell_tracer_user
POSTGRES_PASSWORD=CHANGE_ME_SECURE_PASSWORD_123
POSTGRES_HOST=postgres  # Use 'postgres' for Docker Compose, 'localhost' for local dev
POSTGRES_PORT=5432
DATABASE_URL=postgresql://dell_tracer_user:CHANGE_ME_SECURE_PASSWORD_123@postgres:5432/port_tracer_db

# =================================================================
# APPLICATION SECURITY (Required)
# =================================================================
# Flask application secret key - generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=CHANGE_ME_GENERATE_RANDOM_SECRET_KEY
FLASK_ENV=production
FLASK_DEBUG=false

# Session security settings
SESSION_COOKIE_SECURE=false  # Set to 'true' for HTTPS in production
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Lax

# Session timeout (in minutes)
PERMANENT_SESSION_LIFETIME=5

# =================================================================
# DELL SWITCH SSH CREDENTIALS (Required for functionality)
# =================================================================
# SSH credentials used to connect to Dell switches
# These must match your Dell switch configuration
SWITCH_USERNAME=admin
SWITCH_PASSWORD=CHANGE_ME_SWITCH_PASSWORD

# =================================================================
# WEB APPLICATION USER PASSWORDS (Optional - Override defaults)
# =================================================================
# If not set, default passwords will be used (NOT recommended for production)
# OSS User (View-only access)
OSS_PASSWORD=oss123

# Network Admin (Full VLAN management access)
NETADMIN_PASSWORD=netadmin123

# Super Admin (Full system access)
SUPERADMIN_PASSWORD=superadmin123

# Legacy Admin User (Maps to SuperAdmin)
WEB_PASSWORD=password

# =================================================================
# PERFORMANCE TUNING (Optional)
# =================================================================
# CPU monitoring thresholds (percentage)
CPU_GREEN_THRESHOLD=40
CPU_YELLOW_THRESHOLD=60
CPU_RED_THRESHOLD=80

# Concurrent switch connections
MAX_CONCURRENT_SWITCHES=8
GLOBAL_MAX_CONCURRENT=64

# =================================================================
# APPLICATION SETTINGS (Optional)
# =================================================================
APP_HOST=0.0.0.0
APP_PORT=5000

# Syslog Configuration (if used)
SYSLOG_HOST=your-syslog-server-here
SYSLOG_PORT=514

# =================================================================
# ACTIVE DIRECTORY INTEGRATION (Optional - Windows Auth)
# =================================================================
# Uncomment and configure for Windows AD authentication
# AD_SERVER=ldap://your-domain-controller.company.com
# AD_DOMAIN=COMPANY
# AD_BASE_DN=DC=company,DC=com
# AD_SERVICE_USER=porttracer-service@company.com
# AD_SERVICE_PASSWORD=SERVICE_ACCOUNT_PASSWORD

# =================================================================
# LOGGING CONFIGURATION (Optional)
# =================================================================
LOG_LEVEL=INFO
LOG_MAX_BYTES=10485760  # 10MB
LOG_BACKUP_COUNT=5

# =================================================================
# DOCKER CONFIGURATION (Optional)
# =================================================================
# Container restart policy
RESTART_POLICY=unless-stopped

# =================================================================
# SECURITY NOTES:
# =================================================================
# 1. Generate strong, unique passwords for all accounts
# 2. Use HTTPS in production (set SESSION_COOKIE_SECURE=true)
# 3. Regularly rotate passwords and secret keys
# 4. Monitor audit.log for security events
# 5. Keep switch SSH credentials secure and rotated
# =================================================================
