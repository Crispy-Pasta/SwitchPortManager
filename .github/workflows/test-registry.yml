# Test GitHub Container Registry Access
name: 🧪 Test Container Registry

on:
  workflow_dispatch:  # Manual trigger for testing

jobs:
  test-registry:
    name: 🧪 Test GHCR Access
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: 📥 Checkout Code
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔑 Login to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 🧪 Test Build Simple Image
        run: |
          echo "FROM alpine:latest" > Dockerfile.test
          echo "RUN echo 'GHCR test successful!'" >> Dockerfile.test
          
          docker build -f Dockerfile.test -t ghcr.io/${{ github.repository_owner }}/test-image:latest .
          docker push ghcr.io/${{ github.repository_owner }}/test-image:latest
          
          echo "✅ Successfully pushed test image to GHCR!"
          
      - name: 🧹 Cleanup
        run: |
          rm -f Dockerfile.test
          echo "✅ Test completed successfully!"
