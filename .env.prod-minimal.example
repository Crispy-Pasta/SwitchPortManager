# =================================================================
# Dell Switch Port Tracer v2.1.8 - Minimal Production Environment
# =================================================================
# For deployments with existing nginx and PostgreSQL services
# Copy this to .env and configure for your environment

# =================================================================
# EXTERNAL DATABASE CONFIGURATION (Required)
# =================================================================
# Connection to your existing PostgreSQL instance
POSTGRES_HOST=localhost  # or your PostgreSQL server IP
POSTGRES_PORT=5432
POSTGRES_DB=port_tracer_db
POSTGRES_USER=dell_tracer_user
POSTGRES_PASSWORD=YOUR_EXISTING_DATABASE_PASSWORD

# =================================================================  
# APPLICATION SECURITY (Required)
# =================================================================
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=YOUR_SECRET_KEY_HERE_GENERATE_WITH_PYTHON_SECRETS

# Production session configuration
SESSION_COOKIE_SECURE=true   # Set to false if not using HTTPS
SESSION_COOKIE_HTTPONLY=true
SESSION_COOKIE_SAMESITE=Strict
PERMANENT_SESSION_LIFETIME=5

# =================================================================
# DELL SWITCH SSH CREDENTIALS (Required)
# =================================================================
SWITCH_USERNAME=admin
SWITCH_PASSWORD=YOUR_SWITCH_PASSWORD_HERE

# =================================================================
# WEB APPLICATION PASSWORDS (Change for security)
# =================================================================
OSS_PASSWORD=oss123
NETADMIN_PASSWORD=netadmin123  
SUPERADMIN_PASSWORD=superadmin123
WEB_PASSWORD=password

# =================================================================
# PERFORMANCE SETTINGS (Optional)
# =================================================================
CPU_GREEN_THRESHOLD=40
CPU_YELLOW_THRESHOLD=60
CPU_RED_THRESHOLD=80
MAX_CONCURRENT_SWITCHES=8
GLOBAL_MAX_CONCURRENT=64
